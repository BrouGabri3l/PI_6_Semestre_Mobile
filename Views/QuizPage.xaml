<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="projeto_pi.Views.QuizPage"
             Title="QuizPage">
    <StackLayout Padding="20" Spacing="10">
        <Label Text="{Binding CurrentQuestion.Text}" FontAttributes="Bold" FontSize="Medium" />
        <CollectionView ItemsSource="{Binding CurrentQuestion.Options}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Label Text="{Binding Text}" Grid.Column="0" />
                        <CheckBox IsChecked="{Binding IsSelected}" Grid.Column="1" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Próximo" Command="{Binding NextCommand}" />
    </StackLayout>
</ContentPage>